# ld-603 消防设施巡检维保平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**消防设施巡检维保平台（fire-inspect）**：面向园区和物业公司的消防设备巡检、隐患整改、维保计划和合规台账系统。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

1. **建筑楼栋（Building）**
   - 字段：id, name, campus, floor_count, fire_grade, manager_id, address_code
   - 关键关系：包含多个 FireDevice 和 InspectionTask
   - 业务动作：楼栋建档、楼层配置、责任人维护
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

2. **消防设备（FireDevice）**
   - 字段：id, building_id, device_code, device_type, floor, location_desc, install_date, status, next_maintenance_at
   - 关键关系：属于 Building，被巡检任务和隐患引用
   - 业务动作：设备登记、二维码编号、状态更新
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

3. **巡检任务（InspectionTask）**
   - 字段：id, building_id, inspector_id, plan_date, task_type, status, checklist_version, finished_at
   - 关键关系：生成多个 InspectionResult
   - 业务动作：排期、领取、提交、复核
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

4. **巡检结果（InspectionResult）**
   - 字段：id, task_id, device_id, item_code, result_status, measured_value, photo_url, note
   - 关键关系：异常结果可生成 HazardTicket
   - 业务动作：录入检查项、拍照、异常判定
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

5. **隐患整改单（HazardTicket）**
   - 字段：id, result_id, severity, owner_id, deadline, rectify_status, rectify_note, closed_at
   - 关键关系：由异常结果触发并反向影响设备状态
   - 业务动作：派单、整改、复验、关闭
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

1. **消防合规总览** `/dashboard`
   - 消费实体/模型：Building + FireDevice + HazardTicket
   - 关键交互：设备状态分布、逾期整改、巡检完成率和高危隐患
   - 共享组件：StatCard, StatusBadge, HazardSeverityTag

2. **消防设备台账** `/devices`
   - 消费实体/模型：FireDevice + Building + InspectionResult
   - 关键交互：设备列表、楼层筛选、维保日期、历史结果
   - 共享组件：StatusBadge, DeviceLocationCell, FilterBar

3. **巡检任务** `/tasks`
   - 消费实体/模型：InspectionTask + FireDevice + InspectionResult
   - 关键交互：创建计划、领取任务、填写检查项、提交复核
   - 共享组件：StatusBadge, ChecklistPanel, TimelineList

4. **隐患整改** `/hazards`
   - 消费实体/模型：HazardTicket + InspectionResult + FireDevice
   - 关键交互：隐患分级、责任人派单、复验关闭
   - 共享组件：HazardSeverityTag, TimelineList, StatusBadge

5. **合规报表** `/reports`
   - 消费实体/模型：InspectionTask + HazardTicket + Building
   - 关键交互：按月生成巡检率、整改率和设备故障率图表
   - 共享组件：StatCard, ChartPanel, EmptyState

### 4. 横切关注点（≥ 2 个）

1. **JWT + RBAC（巡检员/维保商/物业主管/审计员）**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
2. **操作日志（巡检提交、隐患派单、复验关闭）**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
3. **全局错误处理 + 接口限流**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。

### 5. 共享枚举/常量（≥ 2 组）

1. **DeviceType**：EXTINGUISHER / HYDRANT / SMOKE_DETECTOR / SPRINKLER / EXIT_LIGHT
   - 前端位置：`frontend/src/constants/DeviceType.ts` 或同名模块
   - 后端位置：`backend/src/constants/DeviceType.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
2. **InspectionStatus**：PLANNED / IN_PROGRESS / SUBMITTED / REVIEWED / OVERDUE
   - 前端位置：`frontend/src/constants/InspectionStatus.ts` 或同名模块
   - 后端位置：`backend/src/constants/InspectionStatus.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
3. **HazardSeverity**：LOW / MEDIUM / HIGH / CRITICAL
   - 前端位置：`frontend/src/constants/HazardSeverity.ts` 或同名模块
   - 后端位置：`backend/src/constants/HazardSeverity.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置

### 6. 共享前端组件清单（≥ 3 个）

- `StatusBadge`
- `HazardSeverityTag`
- `ChecklistPanel`
- `DeviceLocationCell`
- `TimelineList`

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useChecklistProgress`
- `useHazardFlow`
- `usePagination`

### 8. 后端中间件清单（≥ 2 个）

- `authMiddleware`
- `rbacMiddleware`
- `auditLogMiddleware`
- `errorHandlerMiddleware`
- `requestLoggerMiddleware`

### 9. 技术要求

- 英文短名（用于 Docker Compose）：`fire-inspect`
- 前端端口：`20103`
- 后端端口：`21103`
- 后端容器内部端口：`8000`
- 前端技术栈：React 18 + TypeScript + Vite + Material UI + Redux Toolkit
- 后端技术栈：FastAPI + Python 3.11 + SQLAlchemy 2.0
- 数据库：PostgreSQL 15
- 认证方式：JWT + RBAC
- 数据来源：全部本地数据库和种子数据，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

前端必须包含：
```text
frontend/src/
├── api/                  # 按实体分文件，每个实体一个 .ts
├── stores/               # 按实体分文件
├── types/                # 共享类型定义
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 按实体拆默认对象和表单构造器
├── components/common/    # 至少 3 个共享组件
├── hooks/                # 至少 2 个自定义 hook
├── pages/                # 至少 4 个页面
├── router/
├── utils/
└── mocks/                # 本地种子数据，禁止第三方 API
```

后端必须包含：
```text
backend/src/
├── routes/               # 按实体分文件
├── controllers/          # 按实体分文件
├── services/             # 按实体分文件
├── models/               # 按实体分文件
├── repositories/         # 数据访问层
├── middlewares/          # 至少 2 个
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 请求/响应 DTO 构造器
├── utils/
├── types/
└── config/
```

严禁合并职责到单一文件。


### 部署要求

这是前后端分离项目，必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，编排 `frontend`、`backend`、`db`，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=fire-inspect`、`FRONTEND_PORT=20103`、`BACKEND_PORT=21103`、`DB_PORT`
- 根目录 `.env.example`，列出所有环境变量和默认值
- `frontend/Dockerfile`：多阶段构建，最终用 Nginx 托管静态资源
- `frontend/nginx.conf`：`location /api/` 反向代理到 `http://backend:8000/`，并配置 `try_files $uri $uri/ /index.html;`
- `backend/Dockerfile`
- `database/init.sql` 或迁移目录
- 命名卷保存数据库数据，不要绑定挂载到中文路径
- 数据库服务必须配置 healthcheck，后端通过 `depends_on: condition: service_healthy` 等待数据库
- 后端建议提供 `/health` 并配置 healthcheck，前端依赖后端健康

`docker-compose.yml` 必须满足：
- 顶层 `name: fire-inspect`
- 所有服务使用 `container_name`，命名带 `${COMPOSE_PROJECT_NAME:-fire-inspect}` 前缀
- 前端端口映射：`${FRONTEND_PORT:-20103}:80`
- 后端端口映射：`${BACKEND_PORT:-21103}:8000`
- 前端代码里统一请求 `/api`，禁止硬编码 `localhost`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d` 后前端访问 `http://localhost:20103`
- 后端健康检查访问 `http://localhost:21103/health`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
